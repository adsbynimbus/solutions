# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Declare Kotlin files as text
*.kt text
*.kts text

# Linux/macOS start script should use lf
/gradlew text eol=lf

# These are Windows script files and should use crlf
*.bat text eol=crlf

# For more information - https://help.github.com/articles/dealing-with-line-endings/
